@page "{id}"
@model ViewToDoModel
@{
	ViewData["Title"] = "View To-Do";
}

<p>
	@if (Model.Item.IsComplete)
	{
		<strong>This task is complete!</strong>
	}
	else
	{
		<partial
			model="Model.Item"
			name="_ToDo" />
	}
</p>